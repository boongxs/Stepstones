<UserControl x:Class="stepstones.Views.EnlargeMediaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:stepstones.ViewModels"
             xmlns:local="clr-namespace:stepstones.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:EnlargeMediaViewModel}"
             d:DesignHeight="600" 
             d:DesignWidth="800">
    <Grid MaxWidth="{Binding OriginalWidth}" 
          MaxHeight="{Binding OriginalHeight}"
          Background="Transparent"
          MouseLeftButtonDown="OnMediaViewMouseDown"
          Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Image>
                <Image.Style>
                    <Style TargetType="Image">
                        <Setter Property="Opacity" Value="0"/>
                        <Setter Property="Source" Value="{x:Null}"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsImage}"
                                             Value="True">
                                <Setter Property="Opacity" Value="1"/>
                                <Setter Property="Source" Value="{Binding FilePath}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>

            <MediaElement x:Name="VideoPlayer"
                              LoadedBehavior="Manual"
                              UnloadedBehavior="Close"
                              MouseLeftButtonDown="VideoPlayer_MouseLeftButtonDown">
                <MediaElement.Style>
                    <Style TargetType="MediaElement">
                        <Setter Property="Opacity" Value="0"/>
                        <Setter Property="Source" Value="{x:Null}"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsVideo}"
                                             Value="True">
                                <Setter Property="Opacity" Value="1"/>
                                <Setter Property="Source" Value="{Binding FilePath}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </MediaElement.Style>
            </MediaElement>

            <MediaElement x:Name="GifPlayer"
                          LoadedBehavior="Manual"
                          UnloadedBehavior="Close"
                          MediaEnded="GifPlayer_MediaEnded">
                <MediaElement.Style>
                    <Style TargetType="MediaElement">
                        <Setter Property="Opacity" Value="0"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsGif}"
                                         Value="True">
                                <Setter Property="Opacity" Value="1"/>
                                <Setter Property="Source" Value="{Binding FilePath}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </MediaElement.Style>
            </MediaElement>
        </Grid>
    </Grid>
</UserControl>
