<Window x:Class="stepstones.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:stepstones"
        xmlns:viewmodels="clr-namespace:stepstones.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="Stepstones"
        Width="1000"
        Height="650"
        MinWidth="700"
        MinHeight="600"
        WindowState="Maximized"
        Background="#282828">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               Content="Stepstones"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontSize="90"
               Foreground="#f0f0f0">
        </Label>

        <TextBox Grid.Row="1"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Width="600"
                 Height="72"
                 Background="#303030"
                 Foreground="#f0f0f0"
                 CaretBrush="#f0f0f0"
                 BorderThickness="0"
                 FontSize="32"
                 VerticalContentAlignment="Center"
                 Margin="0,20,0,20"
                 Padding="10,0,0,0">
        </TextBox>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0,0,0,10">
            <Button Content="Sel"
                    Command="{Binding SelectFolderCommand}"
                    Width="50"
                    Height="50"
                    Background="#404040"
                    Foreground="#f0f0f0"
                    BorderThickness="0"
                    Margin="5">
            </Button>

            <Button Content="Upl"
                    Command="{Binding UploadFilesCommand}"
                    Width="50"
                    Height="50"
                    Background="#404040"
                    Foreground="#f0f0f0"
                    BorderThickness="0"
                    Margin="5">
            </Button>
        </StackPanel>

        <ScrollViewer Grid.Row="3"
                      Margin="20,0,20,20"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="MediaItemsControl"
                          ItemsSource="{Binding MediaItems}"
                          SizeChanged="MediaItemsControl_SizeChanged"
                          VerticalAlignment="Top">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding GridColumns}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid x:Name="ItemContainer"
                              Margin="5"
                              Height="250"
                              Background="#404040">
                            <!-- Layer 1: Placeholder border -->
                            <Border Background="#404040"/>

                            <!-- Layer 2: Thumbnail -->
                            <Image Source="{Binding ThumbnailPath}"
                                   Stretch="UniformToFill">
                            </Image>
                            
                            <!-- Layer 3: Darkening overlay -->
                            <Border Background="#80000000">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMouseOver, ElementName=ItemContainer}"
                                                         Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                            
                            <!-- Layer 4: Command buttons overlay -->
                            <Grid>
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMouseOver, ElementName=ItemContainer}"
                                                         Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Row="0"
                                        Grid.Column="0"
                                        Style="{StaticResource QuadrantIconButtonStyle}"
                                        Tag="/Resources/Icons/copy.svg"
                                        Content="/Resources/Icons/copy-hover.svg">
                                </Button>

                                <Button Grid.Row="0"
                                        Grid.Column="1"
                                        Style="{StaticResource QuadrantIconButtonStyle}"
                                        Tag="/Resources/Icons/edit.svg"
                                        Content="/Resources/Icons/edit-hover.svg">
                                </Button>

                                <Button Grid.Row="1"
                                        Grid.Column="0"
                                        Style="{StaticResource QuadrantIconButtonStyle}"
                                        Tag="/Resources/Icons/enlarge.svg"
                                        Content="/Resources/Icons/enlarge-hover.svg">
                                </Button>

                                <Button Grid.Row="1"
                                        Grid.Column="1"
                                        Style="{StaticResource QuadrantIconButtonStyle}"
                                        Tag="/Resources/Icons/delete.svg"
                                        Content="/Resources/Icons/delete-hover.svg">
                                </Button>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
