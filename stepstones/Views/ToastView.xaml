<UserControl x:Class="stepstones.Views.ToastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:stepstones.ViewModels"
             xmlns:enums="clr-namespace:stepstones.Enums"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:ToastViewModel}"
             HorizontalAlignment="Center"
             VerticalAlignment="Top">
    <UserControl.RenderTransform>
        <TranslateTransform Y="-100"/>
    </UserControl.RenderTransform>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                     To="20"
                                     Duration="0:0:0.3">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>

                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                     To="-100"
                                     Duration="0:0:0.3"
                                     BeginTime="0:0:2.5">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseIn"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Border Padding="30,15"
            Margin="10"
            MinWidth="250"
            CornerRadius="5">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#2196F3"/>

                <Style.Triggers>
                    <DataTrigger Binding="{Binding Type}"
                                 Value="{x:Static enums:ToastNotificationType.Success}">
                        <Setter Property="Background" Value="#4CAF50"/>
                    </DataTrigger>

                    <DataTrigger Binding="{Binding Type}"
                                 Value="{x:Static enums:ToastNotificationType.Error}">
                        <Setter Property="Background" Value="#F44336"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <TextBlock Text="{Binding Message}"
                   Foreground="White"
                   FontSize="20"
                   TextAlignment="Center">
        </TextBlock>
    </Border>
</UserControl>
