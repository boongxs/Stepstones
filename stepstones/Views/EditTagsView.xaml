<UserControl x:Class="stepstones.Views.EditTagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:stepstones.ViewModels"
             xmlns:views="clr-namespace:stepstones.Views"
             xmlns:local="clr-namespace:stepstones.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:EditTagsViewModel}"
             d:DesignHeight="150" d:DesignWidth="400">
    <Border Background="#282828"
            Padding="15"
            Width="700"
            Height="450"
            CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Tags"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="5,0,0,10">
            </TextBlock>

            <TextBox x:Name="TagsTextBox"
                     Grid.Row="1"
                     Text="{Binding TagsText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Background="#303030"
                     Foreground="#f0f0f0"
                     CaretBrush="#f0f0f0"
                     BorderThickness="0"
                     FontSize="24"
                     Padding="5"
                     Margin="5">
            </TextBox>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,10,0,0">
                <Button x:Name="CancelButton"
                        Style="{StaticResource StackPanelButtonStyle}"
                        Content="Cancel"
                        Width="80"
                        Height="40"
                        Margin="5"
                        FontSize="16"
                        Command="{Binding DataContext.CloseDialogCommand, RelativeSource={RelativeSource AncestorType=views:DialogHost}}"
                        CommandParameter="Cancel">
                </Button>

                <Button x:Name="SaveButton"
                        Style="{StaticResource StackPanelButtonStyle}"
                        Content="Save"
                        Width="80"
                        Height="40"
                        Margin="5"
                        FontSize="16"
                        Command="{Binding DataContext.CloseDialogCommand, RelativeSource={RelativeSource AncestorType=views:DialogHost}}"
                        CommandParameter="Save">
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
