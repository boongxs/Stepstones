<UserControl x:Class="stepstones.Views.EnlargeImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:stepstones.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:EnlargeImageViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid MaxWidth="{Binding OriginalWidth}"
          MaxHeight="{Binding OriginalHeight}"
          Background="Transparent"
          Margin="20">
        <Image x:Name="ImageViewer"
               Source="{Binding ImageSource}">
        </Image>
    </Grid>
</UserControl>
