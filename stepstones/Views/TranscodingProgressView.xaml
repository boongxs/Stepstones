<UserControl x:Class="stepstones.Views.TranscodingProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:stepstones.ViewModels"
             xmlns:res="clr-namespace:stepstones.Resources"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:TranscodingProgressViewModel}"
             d:DesignHeight="150" 
             d:DesignWidth="400">
    <Border Background="#282828"
            BorderBrush="#505050"
            BorderThickness="1"
            Padding="25"
            CornerRadius="5">
        <TextBlock Text="{x:Static res:AppConstants.TranscodingProgressText}"
                   Foreground="White"
                   FontSize="18"
                   TextWrapping="Wrap">
        </TextBlock>
    </Border>
</UserControl>
