<UserControl x:Class="stepstones.Views.EnlargeGifView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:stepstones.ViewModels"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:EnlargeGifViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid MaxWidth="{Binding OriginalWidth}"
          MaxHeight="{Binding OriginalHeight}"
          Background="Transparent"
          Margin="20">
        <Image gif:ImageBehavior.AnimatedSource="{Binding FilePath}"/>
    </Grid>
</UserControl>
